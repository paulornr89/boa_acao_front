import { Outlet, Link } from 'react-router-dom';

export default function Dashboard() {
    return (
        <div className="flex justify-center items-center h-[80vh]">
            <h1>Bem-vindo ao Boa-Ação</h1>
        </div>
    );
}